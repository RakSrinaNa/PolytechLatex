deploy:
  script:
    - apt-get update 
    - apt-get install zip
    - mkdir polytech
    - find . -maxdepth 1 -type f -not -name ".?*" -exec mv {} polytech/ \;
    - zip -R -x .git polytech.zip polytech
  artifacts:
    paths:
      - polytech.zip
